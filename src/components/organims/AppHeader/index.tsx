import { Box } from '@chakra-ui/layout';
import React from 'react';

interface IProps {}

const AppHeader: React.FC<IProps> = (props) => {
  return <Box>AppHeader</Box>;
};

export default AppHeader;
